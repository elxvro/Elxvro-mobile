ELXVRO GitHub Actions fix
=========================

Bu proje Flutter projesidir. npm install CALISTIRILMAMALIDIR.

GitHub deposunda .github/workflows klasorunu kontrol et.
Sadece bu dosya kalabilir:
  .github/workflows/build-apk.yml

Asagidaki gibi eski workflow dosyalari varsa GitHub'dan sil:
  npm.yml
  node.yml
  node.js.yml
  build.yml (icinde npm install varsa)
  ci.yml (icinde npm install varsa)

Daha sonra:
1. Yeni paketin ICINDEKI dosyalari repository root'una yukle.
2. Actions > Build ELXVRO Android APK ac.
3. Run workflow'a bas.
4. Build bitince Artifacts > ELXVRO-APK-v1.0.1 indir.
5. ZIP icindeki app-release.apk Android cihazina kurulabilir.

Not: package.json bu proje icin gerekli degildir.
